name: truehub
description: "A Flutter app to manage TrueNAS servers."
license: AGPL-3.0
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.4.1+11

environment:
  sdk: ^3.9.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.9

  # HTTP client for API calls
  dio: ^5.11.0

  # JSON-RPC client
  json_rpc_2: ^4.1.0

  # Local storage
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  path_provider: ^2.1.6
  path: ^1.9.1
  # database.dart's migration steps catch SqliteException directly to make
  # themselves idempotent against a partially-applied prior run - already a
  # transitive dep of drift, listed explicitly since it's imported directly.
  sqlite3: ^3.5.2

  # State management
  provider: ^6.1.5+1

  # Utilities
  equatable: ^2.1.0
  uuid: ^4.6.0

  # UI enhancements
  flutter_slidable: ^4.0.3
  stream_channel: ^2.1.4

  # WebSocket support
  web_socket_channel: ^3.0.3

  # Network connectivity detection
  connectivity_plus: ^7.3.1
  network_info_plus: ^8.2.1
  permission_handler: ^13.0.1

  # System tray/menu bar support
  tray_manager: ^0.5.3

  # Secure credential storage
  flutter_secure_storage: ^11.0.0
  local_auth: ^3.0.2

  # URL launcher for opening app URLs
  url_launcher: ^6.3.2

  # Navigation packages for adaptive sidebar
  go_router: ^18.0.0
  cupertino_sidebar: ^1.0.1

  # Native plugins for CloudKit and Keychain
  truenas_native_plugins:
    path: packages/truenas_native_plugins

  # OpenTelemetry SDK (logs signal). Pinned to a branch ref because
  # flutter-otel isn't merged/published yet - remove the ref pin above and
  # the dependency_overrides below, and pick a real published version, once
  # it lands on `main` or pub.dev.
  flutter_otel:
    git:
      url: https://github.com/cedricziel/flutter-otel.git
      ref: main
      path: packages/flutter_otel

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.5
  build_runner: ^2.16.0
  dart_pre_commit: ^6.1.4

  # Platform-interface test doubles for SecureStorageService's tests -
  # both are already transitive deps of flutter_secure_storage/local_auth;
  # listed explicitly since the tests import them directly.
  flutter_secure_storage_platform_interface: ^2.0.3
  local_auth_platform_interface: ^1.1.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icons/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# flutter_otel's own pubspec.yaml declares these three siblings as plain
# `^0.1.0` version constraints, which only resolve automatically inside the
# flutter-otel Dart workspace itself. trueapp is a separate workspace, so
# without these overrides pub tries (and fails) to find them on pub.dev.
# Pin all four to the same repo/ref/path as the `flutter_otel` dependency
# above, and drop this block in the same cleanup mentioned there.
dependency_overrides:
  flutter_otel_api:
    git:
      url: https://github.com/cedricziel/flutter-otel.git
      ref: main
      path: packages/flutter_otel_api
  flutter_otel_sdk:
    git:
      url: https://github.com/cedricziel/flutter-otel.git
      ref: main
      path: packages/flutter_otel_sdk
  flutter_otel_exporter_otlp_http:
    git:
      url: https://github.com/cedricziel/flutter-otel.git
      ref: main
      path: packages/flutter_otel_exporter_otlp_http

dart_pre_commit:
  format: true
  analyze: true
  outdated: true
